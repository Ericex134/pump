import { View, Text, FlatList } from "react-native";
import React from "react";

const Streaks = ({ users }) => {
  return (
    <View>
      <Text>TEST</Text>
    </View>
  );
};

export default Streaks;
